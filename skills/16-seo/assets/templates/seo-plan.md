# Plan SEO — {nom du projet}
> Étape 16 (Marketing). SEO de base, **qualité mesurée**. Deux livrables : (a) socle technique **committé sans gate**, (b) pages éditoriales **gatées**. {date}

## Livrable (a) — Socle technique (code committé, sans gate humain)
- `app/sitemap.ts` + `app/robots.ts` (bloque les zones auth) : {commit}
- Metadata + canonical **par route** : {état}
- JSON-LD (`schema-dts`, type M6 selon le type de site) : {types}
- Core Web Vitals (`unlighthouse`) : {LCP / CLS / INP}
- Livret QA (`qa/test-booklet.md`, section « SEO technique ») : {rempli le …}

## Livrable (b) — Contenu éditorial (plan + gate humain)
### Mots-clés & clusters
{renvoi à `seo/topic-cluster-map.md` — data : fallback gratuit par défaut, upgrade optionnel si provisionné (M1)}

### Pages (MESURÉ — plafond : landing + 3-8)
| Page | Cluster / intention | Mot-clé principal | Self-assessment Helpful Content |
|---|---|---|---|
| Landing | … | … | PASS / à revoir |
| … | … | … | … |

### On-page (3-B)
- Meta / Hn / URLs / OG / maillage : {état}
- Extension du socle aux pages retenues (sitemap, canonical, JSON-LD) : {état}

## Garde-fous
- Plafond de pages respecté : {n}/{cap}
- Self-assessment Helpful Content : {n} PASS
- **Gate humain (pages éditoriales)** : {validé le … / « prêt, en attente » — le socle reste committé}

## Itération (post-lancement, GSC si provisionné)
{pistes, dans la limite mesurée}
